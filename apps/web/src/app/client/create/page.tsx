import CreateClientForm from '@/components/client/CreateClient';
import Wrapper from '@/components/wrapper/wrapper';

export default function CreateClient() {
  return (
    <Wrapper>
      <CreateClientForm />
    </Wrapper>
  );
}
